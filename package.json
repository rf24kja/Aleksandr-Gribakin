{
  "name": "gribakin-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive scroll-driven portfolio by Aleksandr Gribakin",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/prerender.js",
    "preview": "vite preview",
    "bot": "node telegram-bot.js",
    "lint": "eslint src/",
    "format": "prettier --write src/",
    "prerender": "node scripts/prerender.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "dependencies": {
    "gsap": "^3.12.2",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "eslint": "^8.56.0",
    "prettier": "^3.2.0",
    "vite": "^5.0.0"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version"
    ]
  }
}
